{
  "name": "fbcli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "fbcli": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "manifest": "tsx src/index.ts help --json",
    "test": "vitest run",
    "ci:check": "npm run build && npm run test"
  },
  "dependencies": {
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
